USE employees;

-- 2b) Recursive CTE: generate numbers from 1 to 10

WITH RECURSIVE numbers_cte (n) AS
(
    SELECT 1

    UNION ALL

    SELECT n + 1
    FROM numbers_cte
    WHERE n < 10
)
SELECT n AS generated_number
FROM numbers_cte;